import { IfWarningDirective } from './if-warning.directive';

describe('IfWarningDirective', () => {
  it('should create an instance', () => {
    const directive = new IfWarningDirective();
    expect(directive).toBeTruthy();
  });
});
