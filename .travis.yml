language: node_js
node_js:
  - "10"

script: 
  - cd latest && npm ci && npm run deploy:ci
  - cd ../

deploy:
  provider: pages
  verbose: true
  local_dir: deploy
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  committer-from-gh: true
  on:
    branch: website